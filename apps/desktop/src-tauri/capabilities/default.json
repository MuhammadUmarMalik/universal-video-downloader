{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal Phase 1 permissions for the desktop shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
